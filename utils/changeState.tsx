export default function changeState(state: any, route: string, value: any): any {
  let newState = {}

  const fullPath = route.split(".")

  for(let i = 0; i < fullPath.length; i++) {
    newState = {
      
    }
  }

  return newState
}